import {View} from 'react-native';
import React from 'react';
import {Icon} from '@rneui/themed';

const HeaderIcon = () => {
  return (
    <View>
      <Icon name="user" type="ant-design" color="#517fa4" onPress={() => {}} />
    </View>
  );
};

export default HeaderIcon;
